item_1 = 8    # 1) create variable item_1 of type integer
item_2 = 3   # 2) create variable item_2 of type integer

result_sum = item_1 + item_2    # 3) create variable result_sum where you sum up item_1 and item_2
print('result_sum =', result_sum)    # 4) output result_sum to the console

result_subtr = item_1 - item_2    # 5) create variable result_subtr and subtract the larger variable from the smaller one
print('result_subtr =', result_subtr)    # 6) output result_subtr to the console

result_multi = item_1 * item_2    # 7) create variable result_multi and multiply the variables item_1, item_2
print('result_multi =', result_multi)    # 8) output result_multi to the console

result_exp = item_1 ** item_2    # 9) create variable result_exp and raise item_1 to the power of item_2
print('result_exp =', result_exp)    # 10) output result_exp to the console

import math
#item_1 **= item_2
result_m_exp = math.pow(item_1, item_2)    # 11) create variable result_m_exp and raise item_1 to the power of item_2 using math from library
print('result_m_exp =', result_m_exp)    # 12) output result_m_exp to the console

result_s_root = item_1 ** (0.5)  # 13) create variable result_s_root and find the square root of the variable item_1, item_2
print('result_s_root =', result_s_root)    # 14) output result_s_root to the console

import math
result_s_root = math.sqrt(item_1)  # 15) create variable result_s_root and find the square root of the variable item_1, item_2 using math from library
print('result_s_root =', result_s_root)    # 16) output result_s_root to the console

import math
result_mp_s_root = math.pow(item_1, 0.5)  # 17) create variable result_mp_s_root and find the square root of the variable item_1, item_2 using math.pow from library
print('result_mp_s_root =', result_mp_s_root)    # 18) output result_mp_s_root to the console

item_1 = 9     # 19) make a variable odd
item_2 = 2     # 20) make a variable even

result_division = item_1 / item_2   # 21) create variable result_division and divide item_1 to item_2
print('result_division =', result_division)    # 22) output result_division to the console












